import { DESIGN_WIDTH } from "./config.js";
import { clamp, lerp } from "./utils.js";
export class Player { constructor(texture, skeletonTexture) { this.texture=texture; this.skeletonTexture=skeletonTexture; this.container=new PIXI.Container(); this.baseY=720; this.x=DESIGN_WIDTH*0.5; this.y=this.baseY; this.vx=0; this.input=0; this.swim=0; this.dead=false; this.deadTimer=0; this.sprite=new PIXI.Sprite(texture); this.sprite.anchor.set(0.5); this.sprite.scale.set(0.52); this.skeleton=new PIXI.Sprite(skeletonTexture); this.skeleton.anchor.set(0.5); this.skeleton.scale.set(0.62); this.skeleton.visible=false; this.skeleton.alpha=0; this.shadow=new PIXI.Graphics(); this.shadow.beginFill(0x000000,0.32); this.shadow.drawEllipse(0,58,44,14); this.shadow.endFill(); this.container.addChild(this.shadow,this.sprite,this.skeleton); this.container.position.set(this.x,this.y); }
setInput(v){ this.input=this.dead?0:v; } kill(){ if(this.dead)return; this.dead=true; this.deadTimer=0; this.input=0; this.sprite.visible=false; this.skeleton.visible=true; this.skeleton.alpha=0; } update(dt,speedFactor=1){ this.swim+=dt*(6.8+speedFactor*1.1); if(!this.dead){ const force=2300,damping=5.1; this.vx+=this.input*force*dt; this.vx-=this.vx*damping*dt; this.vx=clamp(this.vx,-880,880); this.x+=this.vx*dt; const margin=145; if(this.x<margin){this.x=margin;this.vx*=-0.18;} if(this.x>DESIGN_WIDTH-margin){this.x=DESIGN_WIDTH-margin;this.vx*=-0.18;} this.y=this.baseY+Math.sin(this.swim*0.85)*9; this.container.rotation=lerp(this.container.rotation,this.vx*0.00055,dt*8); this.sprite.scale.x=0.52*(1+Math.sin(this.swim*1.9)*0.025); this.sprite.scale.y=0.52*(1-Math.sin(this.swim*1.9)*0.018); this.sprite.rotation=Math.sin(this.swim*2.8)*0.025; } else { this.deadTimer+=dt; this.vx-=this.vx*3.6*dt; this.x+=this.vx*dt; this.y+=28*dt; this.container.rotation=lerp(this.container.rotation,Math.sin(this.deadTimer*2.2)*0.22,dt*3); this.skeleton.alpha=lerp(this.skeleton.alpha,1,dt*5); this.skeleton.scale.set(0.62*(1+Math.sin(this.deadTimer*1.9)*0.025)); } this.container.position.set(this.x,this.y); }
getHitCircles(){ return [{x:this.x,y:this.y-22,r:42},{x:this.x,y:this.y-80,r:27},{x:this.x,y:this.y+48,r:28}]; } destroy(){ this.container.destroy({children:true}); } }
