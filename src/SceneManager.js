export class SceneManager { constructor(game){ this.game=game; this.current=null; } async set(SceneClass,data={}){ if(this.current){ this.current.destroy(); this.current=null; } this.current=new SceneClass(this.game,data); this.game.stage.addChild(this.current.container); if(this.current.enter) await this.current.enter(); } update(dt){ if(this.current?.update)this.current.update(dt); } }
